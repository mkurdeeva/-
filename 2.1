#include <iostream>
using namespace std;

int main()
{
    double s = 0, v = 0, p = 0, k = 0;
    cout << "Введите значение площади крыла самолета S:" << endl;
    cin >> s;
    cout << "Введите значение скорости самолета V:" << endl;
    cin >> v;
    cout << "Введите значение плотности воздуха p:" << endl;
    cin >> p;
    cout << "Введите значение коэффициента подъемной силы k:" << endl;
    cin >> k;
    cout << "Подъемная сила равняется: " << 0.5 * p * pow(v, 2) * s * k;
}
